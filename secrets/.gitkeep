# This directory holds the encrypted secrets vault at runtime (vault.enc,
# vault_meta.json). Its contents are gitignored; only this placeholder is
# tracked so the path exists. Never commit anything else from here.
